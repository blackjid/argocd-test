apiVersion: batch/v1
kind: Job
metadata:
  name: myjob-sync
  annotations:
    argocd.argoproj.io/hook: Sync
spec:
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: alpine
        command:
        - sleep
        - "10"
      restartPolicy: Never